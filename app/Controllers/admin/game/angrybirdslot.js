let get_data = require('./angrybirdslot/get_data');
let get_top  = require('./angrybirdslot/get_top');
let name_hu  = require('./angrybirdslot/name_hu');
let setChedo = require('./angrybirdslot/setChedo');
let reset_top  = require('./angrybirdslot/reset_top');
let reset_data = require('./angrybirdslot/reset_data');

module.exports = function(client, data) {
	/* if (void 0 !== data.get_data) {
		get_data(client)
	}
	if (void 0 !== data.name_hu) {
		name_hu(client, data.name_hu)
	}
	if (void 0 !== data.get_top) {
		get_top(client, data.get_top)
	}
	if (void 0 !== data.chedo) {
		setChedo(client, data.chedo);
	}
	if (void 0 !== data.reset_top) {
		reset_top(client);
	}
	if (void 0 !== data.reset_data) {
		reset_data(client);
	} */
}
